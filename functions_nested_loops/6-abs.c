#include "main.h"

/**
 * _abs -  prints the sign of a number.
 * @n: The integer to be computed.
 * Return: absolute value of an integer.
 */
int _abs(int n)
{
	if (n >= 0)
	return (n);
	else
	return (-n);
}
